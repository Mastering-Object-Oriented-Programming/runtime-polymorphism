public class ProductService {

    //at Runtime Product can change to (Mobile, Camera, SmartPhone, Laptop) - based on arguments we pass
    void productDetails(Product product){
        product.displayDetails();
    }
}
